import type { Testimonial } from "@/lib/types/content";

/**
 * Intentionally empty. Per brand guidelines, ISLAND SOL never fabricates
 * customer testimonials or reviews. Populate this from real customer
 * feedback (via CMS) once available — the Testimonials section hides
 * itself automatically when this array is empty.
 */
export const TESTIMONIALS: Testimonial[] = [];
